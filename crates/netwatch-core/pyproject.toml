[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "netwatch-core"
version = "0.1.0"
requires-python = ">=3.10"

[tool.maturin]
module-name = "netwatch_core"
features = ["pyo3/extension-module"]
